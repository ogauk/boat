name: 'Get Boat'
description: 'Get a boats data'
inputs:
  repo:  # id of input
    description: 'repository to update'
    required: true
  oga-no:  # id of input
    description: 'OGA No'
    required: true
  op:  # id of input
    description: 'DELETE, INSERT or UPDATE'
    required: true
outputs:
  sha: # id of output
    description: 'sha of committed data'
runs:
  using: 'node12'
  main: 'dist/index.js'
